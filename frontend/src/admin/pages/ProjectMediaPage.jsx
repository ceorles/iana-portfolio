import ResourceManager from '../components/ResourceManager'
import { projectMediaConfig } from '../config/resourceConfigs'

export default function ProjectMediaPage() {
  return <ResourceManager config={projectMediaConfig} />
}
