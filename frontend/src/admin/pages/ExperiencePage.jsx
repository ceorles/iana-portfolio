import ResourceManager from '../components/ResourceManager'
import { experienceConfig } from '../config/resourceConfigs'

export default function ExperiencePage() {
  return <ResourceManager config={experienceConfig} />
}
